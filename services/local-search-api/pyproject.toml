[project]
name = "slas-local-search-api"
version = "0.0.0"
description = "Internal corpus search (Option A) (CLAUDE.md §8.3)"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "slas_local_search_api"
module-root = ""
