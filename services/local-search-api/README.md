# services/local-search-api

Internal-corpus hybrid search (CLAUDE.md §8.3 Option A). P5.

This directory is a placeholder from Phase 0; the service is built in the phase named above.
Its image (`images/local-search-api/Dockerfile`) answers `/health` and `/metrics` only, with
`python -m slas_observability.serve local-search-api`, until then.
