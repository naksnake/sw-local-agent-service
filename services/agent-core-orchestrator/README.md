# services/agent-core-orchestrator

Runs the Agent Kernel: ingest → ticket → plan → act → logs → RCA → SOP. Never touches hardware directly (CLAUDE.md §11). P2.

This directory is a placeholder from Phase 0; the service is built in the phase named above.
