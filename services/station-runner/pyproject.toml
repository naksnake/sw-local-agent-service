[project]
name = "slas-station-runner"
version = "0.0.0"
description = "Daemon on a physical test station (CLAUDE.md §5.2)"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "slas_station_runner"
module-root = ""
