"""slas_factory_executor: the deterministic loop that drives test stations through the
station runner (CLAUDE.md §4.1 factory zone, §10.3, INV-3). Import from the submodules:
`primitives`, `templates`, `mes`, `backup`, `executor`.
"""
