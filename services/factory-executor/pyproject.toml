[project]
name = "slas-factory-executor"
version = "0.0.0"
description = "Deterministic executor for factory jobs (CLAUDE.md §10.3)"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "slas_factory_executor"
module-root = ""
