# services/git-broker

The only holder of Git credentials and the only route to Git remotes (CLAUDE.md §5.7, INV-14). P6.

This directory is a placeholder from Phase 0; the service is built in the phase named above.
