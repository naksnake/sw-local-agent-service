[project]
name = "slas-validation-executor"
version = "0.0.0"
description = "Deterministic executor for validation runs (CLAUDE.md §10.2)"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "slas_validation_executor"
module-root = ""
