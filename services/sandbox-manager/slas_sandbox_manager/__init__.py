"""slas_sandbox_manager: rootless sandboxes for the Coding Agent (CLAUDE.md §4.1 Zone A).

Import from the submodules. `slas_sandbox_manager.toolchains` is standard-library only so
the host CLI can use it before anything is installed.
"""
