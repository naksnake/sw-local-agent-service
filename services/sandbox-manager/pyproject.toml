[project]
name = "slas-sandbox-manager"
version = "0.0.0"
description = "Creates code sandboxes and runs their exec API (CLAUDE.md §4.1)"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "slas_sandbox_manager"
module-root = ""
