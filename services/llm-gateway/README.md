# services/llm-gateway

The only component that talks to vLLM. Role routing, redaction, guided_json enforcement, circuit breaker, Consensus Router (CLAUDE.md §5.3). P3.

This directory is a placeholder from Phase 0; the service is built in the phase named above.
