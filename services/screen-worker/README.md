# services/screen-worker

Xvfb + x11vnc/noVNC + screen driver, one display per session. Never the host display (INV-4, ADR-0002). P4.

This directory is a placeholder from Phase 0; the service is built in the phase named above.
