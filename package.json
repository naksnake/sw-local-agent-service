{
  "name": "slas-workspace",
  "private": true,
  "version": "0.0.1",
  "description": "SW Local Agent Service — JavaScript workspace root (apps/webui). Python lives in pyproject.toml.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
