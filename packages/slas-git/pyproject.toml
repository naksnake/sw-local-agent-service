[project]
name = "slas-git"
version = "0.0.0"
description = "Git broker library: remotes, gate, bundles (CLAUDE.md §5.7)"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "slas_git"
module-root = ""
