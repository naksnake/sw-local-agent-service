[project]
name = "slas-authz"
version = "0.0.1"
description = "Roles and capabilities; authz runs where the action executes"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
