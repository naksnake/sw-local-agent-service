"""The `slas` command line, mirroring the WebUI (CLAUDE.md §3).

Phase 0 ships `slas doctor`, the preflight that `./install.sh` runs. The other commands
listed in §3 are registered and say which phase brings them.
"""

__version__ = "0.0.1"
