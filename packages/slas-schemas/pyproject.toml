[project]
name = "slas-schemas"
version = "0.0.1"
description = "Shared schemas: Job, Plan, Step, Ticket, Vote, SopModel, Finding, three-part errors"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
