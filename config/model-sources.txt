# Where the model weights come from, for scripts/fetch_models.py on a CONNECTED build host.
# One line per model:  <path> <owner/repo> [revision]
#   <path> is the directory name under Models/ and the `path` field in Models/models.yaml.
#   [revision] pins a branch, tag or commit; the fetched commit is recorded in manifest.json.
# The platform host never reads this file and never downloads anything (INV-1).
#
# Lines that still say TODO are refused by the script: fill in the hub repository id for the
# exact build you want (FP8 or FP4 for Blackwell, per CLAUDE.md §7), then remove the TODO.

# Embedding and reranking (unchanged in the current plan)
bge-m3               BAAI/bge-m3
bge-reranker-v2-m3   BAAI/bge-reranker-v2-m3

# Planner and triage — DeepSeek-V4 Pro (FP4+FP8) and Flash: fill in the repository ids
# deepseek-v4-pro-fp4     TODO owner/repo
# deepseek-v4-flash-fp4   TODO owner/repo

# Coder — Qwen3.8-27B: the FP8 serving build, and the BF16 copy kept for eval regression
# qwen3.8-27b-fp8         TODO owner/repo
# qwen3.8-27b-bf16        TODO owner/repo
