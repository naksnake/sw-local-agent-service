{
  "name": "slas-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests (CLAUDE.md §11)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "playwright test --list",
    "list": "playwright test --list",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "@types/node": "22.15.30",
    "typescript": "5.8.3"
  }
}
